// -------------------------------------------
//   Manage DoT templates
// -------------------------------------------

Template = {
  get: function(template) {
    return JST["templates/" + template];
  }
}
